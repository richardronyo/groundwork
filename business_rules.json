{
  "/Users/richardlui/School/groundwork/flask/docs/conf.py": [
    "As a documentation maintainer, I want the default reStructuredText role to be 'code' so that inline text is rendered as code by default.",
    "As a documentation maintainer, I want autodoc to order members by source so that the generated documentation follows the source code's natural order.",
    "As a documentation maintainer, I want autodoc to place type hints in the description so that function signatures remain concise and detailed type information appears in the body.",
    "As a documentation maintainer, I want autodoc to preserve default parameter values so that the documentation shows accurate defaults for function parameters.",
    "As a documentation maintainer, I want predefined extlinks for issues, pull requests, and security advisories so that authors can reference GitHub resources with short, consistent inline links.",
    "As a documentation maintainer, I want intersphinx mappings to Python and key dependencies (Werkzeug, Click, Jinja, itsdangerous, SQLAlchemy, WTForms, Blinker) so that cross-references to external project documentation resolve correctly.",
    "As a documentation maintainer, I want to use the 'flask' HTML theme and disable the index sidebar logo so that the site uses the project's theme and the index sidebar avoids showing a redundant logo.",
    "As a documentation visitor, I want prominent project links (Donate, PyPI Releases, Source Code, Issue Tracker, Chat) available in the HTML context so that I can quickly find key project resources and contribution channels.",
    "As a documentation maintainer, I want tailored sidebars for the index and other pages so that navigation elements are optimized for the index page and for general content pages respectively.",
    "As a documentation maintainer, I want single-page HTML builds to use a specific sidebar layout for the index so that the consolidated documentation still provides appropriate navigation and project context.",
    "As a documentation maintainer, I want a designated static path and configured favicon and logo so that the generated docs include the project's branded assets.",
    "As a documentation visitor, I want the HTML title to include the documentation version so that I can immediately see which release the documentation corresponds to.",
    "As a documentation maintainer, I want to hide the 'view source' link in the HTML output so that raw source files are not exposed to readers by default.",
    "As a localization maintainer, I want gettext to use UUIDs and to disable compact message catalogs so that translations remain stable and entries are not merged inappropriately.",
    "As a documentation author, I want the custom github_link role to parse text of the form 'label<ref>' and construct a link to the GitHub tree for the configured release so that references point to the correct file or directory in the repository for the current release."
  ],
  "/Users/richardlui/School/groundwork/flask/docs/make.bat": [
    "As a documentation maintainer, I want the script to default SPHINXBUILD to 'sphinx-build' when the SPHINXBUILD environment variable is not set so that the build can run with a sensible default.",
    "As a documentation maintainer, I want the script to set SOURCEDIR to '.' and BUILDDIR to '_build' so that source and output locations are consistent for builds.",
    "As a documentation maintainer, I want the script to display help when no target argument is provided so that I can learn available build targets and usage.",
    "As a documentation maintainer, I want the script to test for the existence of the sphinx-build command and exit with an error and clear instructions if it's missing so that I know how to install or configure Sphinx before proceeding.",
    "As a documentation maintainer, I want the script to invoke sphinx-build with the specified make target and any provided options so that the requested documentation build action is executed.",
    "As a documentation maintainer, I want the script to run sphinx-build -M help when the help path is triggered so that detailed help from Sphinx is displayed.",
    "As a developer, I want the script to change to the script's directory at start and restore the previous directory at the end so that relative paths are resolved correctly during the build."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/celery/src/task_app/__init__.py": [
    "As a developer, I want the application to initialize Celery with default settings (broker_url and result_backend set to redis://localhost and task_ignore_result=True) so that tasks have a working broker/backend and unnecessary task results are not stored.",
    "As a developer, I want the application to allow configuration to be overridden from prefixed environment variables so that deployment-specific settings can replace defaults without changing code.",
    "As a developer, I want Celery tasks to be executed within the Flask application context (via a FlaskTask wrapper) so that tasks can safely access Flask resources like configuration, database connections, and extensions.",
    "As a developer, I want the Celery app to be set as the default Celery application and stored in app.extensions['celery'] so that the Celery instance is discoverable and reusable across the Flask application.",
    "As a site visitor, I want the root URL (/) to render the index.html template so that I can view the application's homepage.",
    "As a developer, I want the application to register the views blueprint during initialization so that the app's routes and view functions are available when the app starts.",
    "As a developer, I want create_app to return a fully configured Flask app (including Celery initialization and blueprint registration) so that the app is ready to be served or tested after creation."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/celery/src/task_app/tasks.py": [
    "As a developer, I want an add task that takes two integers and returns their sum so that I can perform simple arithmetic asynchronously and retrieve the result.",
    "As a developer, I want the add and process tasks to preserve and return results (ignore_result=False) so that I can query their outcomes after execution.",
    "As a developer, I want a block task that sleeps for 5 seconds and does not return a value so that I can simulate or run a fire-and-forget blocking operation.",
    "As a client, I want the process task to update its state to \"PROGRESS\" with meta {\"current\": <n>, \"total\": <total>} on each iteration so that I can monitor the task's progress in real time.",
    "As a developer, I want the process task to iterate exactly `total` times, sleeping one second per iteration and incrementing the current count each loop so that the task models a predictable time-consuming workload.",
    "As a client, I want the process task to return a final result object {\"current\": total, \"total\": total} when complete so that I can confirm the task finished and processed all units.",
    "As a developer, I want tasks to accept integer parameters for arithmetic and iteration inputs so that inputs are consistent with the expected operations."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/celery/src/task_app/views.py": [
    "As a developer, I want to retrieve a task's status by its id so that I can determine whether the task is ready and inspect its outcome.",
    "As a developer, I want the system to only report a task as successful when the task is ready so that success is not reported prematurely.",
    "As a developer, I want the system to return the task's value using a blocking get() when the task is ready and otherwise return the current result attribute so that I can obtain the final value when available and see interim/error information while it's still running.",
    "As a developer, I want to submit an asynchronous add task by providing form fields 'a' and 'b' so that the system queues an add operation and returns a task id for tracking.",
    "As a developer, I want the add endpoint to parse 'a' and 'b' from form data as integers so that the add task receives correctly typed numeric inputs.",
    "As a developer, I want to invoke a blocking/background task via the block endpoint and receive its task id so that I can start long-running work and later query its status.",
    "As a developer, I want to start a process task with an optional 'total' integer provided in form data so that the processing job can use the specified total value and I receive a task id to monitor progress.",
    "As a developer, I want all endpoints that create tasks to return the Celery task id in their response so that I can later query the task's result endpoint using that id.",
    "As a developer, I want form parameters that are missing or non-numeric to be handled by the request parser (yielding None) so that tasks can receive None or default behavior when inputs are absent or invalid."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/javascript/js_example/views.py": [
    "As a user, I want the app to render a JavaScript example template matching the URL segment (xhr, jquery, fetch) so that I can view the requested example page.",
    "As a user, I want the app to default to the 'fetch' example when no js segment is provided so that I get a sensible default page.",
    "As a user, I want the app to restrict templates to the values 'xhr', 'jquery', or 'fetch' so that invalid or unexpected template names are not rendered.",
    "As a user, I want to send a POST to /add with form fields 'a' and 'b' parsed as floats so that numeric addition is performed predictably.",
    "As a user, I want missing or non-numeric 'a' or 'b' inputs to be treated as 0 so that the addition still succeeds without errors.",
    "As a user, I want the /add endpoint to only accept POST requests so that additions are performed intentionally and not via GET.",
    "As a user, I want the /add endpoint to return the sum as JSON in a 'result' field so that client-side code can consume the calculation result easily."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/javascript/tests/test_js_example.py": [
    "As a customer, I want the system to render fetch.html when I visit / so that I see the fetch-based example page.",
    "As a customer, I want the system to render xhr.html when I visit /plain so that I see the plain XHR example page.",
    "As a customer, I want the system to render fetch.html when I visit /fetch so that I see the fetch-based example page.",
    "As a customer, I want the system to render jquery.html when I visit /jquery so that I see the jQuery-based example page.",
    "As a customer, I want to POST form fields 'a' and 'b' to /add and receive a JSON response with a 'result' field so that I can programmatically get the arithmetic result.",
    "As a customer, I want the /add endpoint to correctly add integer and floating-point values so that inputs like 2.5 and 3 return 5.5.",
    "As a customer, I want the /add endpoint to treat a missing or non-numeric 'b' as if no second operand was provided so that posting a with b as null or non-numeric returns the value of a."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/flaskr/__init__.py": [
    "As a developer, I want the application to be created with default configuration values (SECRET_KEY set to 'dev' and DATABASE set to instance/flaskr.sqlite) so that the app has sensible defaults for local development.",
    "As an app administrator, I want the default SECRET_KEY to be overrideable by an instance config so that I can provide a secure secret in production or per-environment settings.",
    "As an app administrator, I want the application to load configuration from an instance config file (config.py) when not testing so that environment-specific settings are applied automatically.",
    "As a tester, I want to be able to pass a test_config to create_app so that tests can run with a separate, controlled configuration.",
    "As an app administrator, I want the application to ensure the instance folder exists (creating it if needed) so that the database file and instance configuration can be stored reliably.",
    "As a user, I want a GET /hello endpoint that returns a simple greeting so that I can verify the application is running or receive a basic health response.",
    "As a developer, I want the database module's initialization routine to be called with the app so that database commands and connections are registered and available in the application context.",
    "As a developer, I want the authentication and blog blueprints to be registered with the application so that authentication and blog routes and functionality are activated.",
    "As a user, I want the root URL (/) to be routed to the blog.index endpoint so that the blog serves as the application's main index page."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/flaskr/auth.py": [
    "As a user, I want the system to redirect anonymous users to the login page when they try to access protected views so that only authenticated users can access those views.",
    "As a user, I want the application to load the logged-in user's data into the request context when a user_id is stored in the session so that views can access the current user's information.",
    "As a user, I want the registration form to require a username so that every account has an identifier.",
    "As a user, I want the registration form to require a password so that accounts cannot be created without credentials.",
    "As a store owner, I want the system to store passwords as cryptographic hashes instead of plain text so that user credentials are protected if the database is compromised.",
    "As a user, I want the system to prevent creating a new account with a username that is already registered so that usernames remain unique.",
    "As a user, I want the system to display validation error messages when registration fails so that I know what needs to be corrected.",
    "As a user, I want the system to redirect me to the login page after successful registration so that I can sign in immediately."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/flaskr/blog.py": [
    "As a visitor, I want to see all posts ordered most recent first so that I can quickly find the latest content.",
    "As a visitor, I want each post to display the author's username so that I know who wrote the post.",
    "As an authenticated user, I want to be required to log in before creating a post so that only registered users can create content.",
    "As an author, I want the system to validate that a post has a title before saving so that posts contain the required information.",
    "As an authenticated user, I want my new post to be saved with my user id as the author so that ownership of content is recorded.",
    "As an author, I want to edit a post only if I am the post's author so that other users cannot modify my content.",
    "As an author, I want the system to validate the title when updating a post so that updates do not leave the post without required data.",
    "As an author, I want to delete a post only by making a POST request and only if I am the author so that deletions are intentional and restricted to owners.",
    "As a user, I want the system to return a 404 error when I request a post id that doesn't exist so that I know the resource is not available.",
    "As a user, I want the system to return a 403 error when I attempt an author-only action for a post I don't own so that unauthorized access is prevented.",
    "As a user, I want to receive clear feedback (flash messages) when an action fails (for example, missing title) so that I understand what I need to fix."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/flaskr/db.py": [
    "As a developer, I want the application to create and reuse a single database connection per request so that database connections are not created unnecessarily and resources are managed efficiently.",
    "As a developer, I want the database connection to be stored in the request context (g) and popped/closed during app teardown so that connections are reliably released at the end of each request.",
    "As a developer, I want query results to use sqlite3.Row as the row factory so that rows can be accessed by column name as well as by index.",
    "As a developer, I want SQLite to parse declared column types (detect_types=sqlite3.PARSE_DECLTYPES) so that database values are converted to appropriate Python types when possible.",
    "As a developer, I want a custom 'timestamp' converter that uses datetime.fromisoformat to decode values so that timestamp fields are converted into Python datetime objects consistently.",
    "As an administrator, I want an init_db function that clears existing data and executes schema.sql to create the database schema so that the database can be initialized to a known state.",
    "As an administrator, I want a CLI command 'init-db' that runs the database initialization and prints a confirmation so that I can initialize the database from the command line.",
    "As a developer, I want init_app to register the database teardown handler and add the init-db CLI command to the Flask app so that cleanup and initialization capabilities are automatically available when the app is created.",
    "As a developer, I want close_db to safely handle the absence of a database connection (pop and check for None) so that attempting to close a non-existent connection does not raise errors."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/flaskr/schema.sql": [
    "As a developer, I want the database initialization to drop existing user and post tables and recreate them so that the database can be reset to a clean state.",
    "As a user, I want the system to require a non-empty, unique username so that accounts are identifiable and duplicate usernames cannot be created.",
    "As a user, I want the system to require a password when creating an account so that accounts cannot be created without credentials and access can be secured.",
    "As an administrator, I want user and post records to have auto-incrementing primary key IDs so that each record is assigned a unique identifier automatically.",
    "As an author, I want every post to require a non-null author_id that references an existing user so that each post is associated with a valid author.",
    "As an author, I want the system to automatically set a post's created timestamp to the current time by default so that the creation time is recorded without manual input.",
    "As an author, I want the system to require a non-empty title and body for each post so that posts contain meaningful content.",
    "As a data steward, I want foreign key constraints enforced between posts and users so that posts cannot reference nonexistent users and referential integrity is maintained."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/conftest.py": [
    "As a test engineer, I want the system to create and configure a new app instance for each test so that tests run in isolation and do not interfere with each other.",
    "As a test engineer, I want the system to use a temporary file as the database for each test so that tests do not share persistent state across runs.",
    "As a test engineer, I want the system to set the app configuration TESTING to True and DATABASE to the temporary path when creating the app so that the app runs in test mode against the isolated database.",
    "As a test engineer, I want the system to initialize the database and execute the predefined test SQL script before tests run so that the database has a known, repeatable dataset for tests.",
    "As a test engineer, I want the system to read the test SQL data file with UTF-8 decoding so that SQL content is interpreted correctly when loaded into the database.",
    "As a test engineer, I want the system to close and remove the temporary database file after each test so that temporary resources are cleaned up and do not persist between test runs.",
    "As a test engineer, I want the system to provide a test client for the app so that tests can simulate HTTP requests and verify application behavior.",
    "As a test engineer, I want the system to provide a CLI runner for the app's Click commands so that tests can invoke and validate command-line functionality.",
    "As a test engineer, I want the system to provide a helper method that logs in by POSTing username and password to /auth/login so that tests can authenticate and exercise authenticated endpoints.",
    "As a test engineer, I want the login helper to default to username 'test' and password 'test' so that tests can easily perform authentication without repeatedly specifying credentials.",
    "As a test engineer, I want the system to provide a helper method that logs out by sending a GET request to /auth/logout so that tests can end authenticated sessions and verify logout behavior."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/data.sql": [
    "As a customer, I want the system to store passwords hashed using PBKDF2-SHA256 with a strong iteration count so that user credentials are protected from compromise.",
    "As a store owner, I want the database seeded with deterministic test users (username and hashed password) so that automated tests can run against known accounts.",
    "As a customer, I want to create posts that include a title, a body, an author_id, and a created timestamp so that each post is attributed and time-stamped for auditing and display.",
    "As a store owner, I want post author_id values to reference existing users so that posts cannot be orphaned and authors are validated.",
    "As a customer, I want the post body to support newline characters so that multi-line content is preserved and rendered correctly.",
    "As a store owner, I want created timestamps stored in a consistent 'YYYY-MM-DD HH:MM:SS' format so that posts can be ordered and compared reliably in tests and the application.",
    "As a store owner, I want the test dataset to include at least one post linked to a test user so that post-related functionality can be exercised during testing."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/test_auth.py": [
    "As a user, I want to be able to view the registration page so that I can see and fill out the registration form.",
    "As a user, I want successful registration requests to redirect me to the login page so that I can sign in immediately.",
    "As a user, I want my registration to create a new user record in the database so that my account exists for future logins.",
    "As a user, I want the system to validate that a username is provided during registration so that accounts cannot be created without a username.",
    "As a user, I want the system to validate that a password is provided during registration so that accounts cannot be created without a password.",
    "As a user, I want the system to prevent registering an already registered username and show an 'already registered' message so that usernames remain unique.",
    "As a user, I want to be able to view the login page so that I can enter my credentials.",
    "As a user, I want successful login to redirect me to the home page so that I am taken to the main site after signing in.",
    "As a user, I want successful login to set my user_id in the session and load my user information into the request context so that the application recognizes me on subsequent requests.",
    "As a user, I want the system to show an 'Incorrect username' message when I enter a non-existent username and an 'Incorrect password' message when I enter a wrong password so that I understand why login failed.",
    "As a user, I want logging out to remove my user_id from the session so that I am no longer authenticated."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/test_blog.py": [
    "As a visitor, I want to see Log In and Register links on the index page so that I can sign in or create an account.",
    "As an authenticated user, I want the index page to display post titles, authors with dates, and post bodies so that I can browse published content.",
    "As an author, I want an edit link for my own posts to appear on the index page so that I can quickly access the post edit page.",
    "As a visitor, I want attempts to create, update, or delete posts to require login so that unauthenticated users cannot modify content.",
    "As a user, I want the system to prevent me from modifying or deleting posts I do not own (returning 403) so that users cannot alter other people's posts.",
    "As an authenticated user, I want attempts to update or delete a non-existent post to return 404 so that I know the resource does not exist.",
    "As an authenticated user, I want to be able to access the create and update pages (GET) and successfully submit them (POST) so that I can create or edit posts.",
    "As an author, I want creating a post to increase the post count in the database so that new posts are persisted and visible.",
    "As an author, I want updating a post to change the post's title in the database so that my edits are saved.",
    "As an authenticated user, I want the system to validate that the title is required and display 'Title is required.' when it is empty so that I provide necessary data for a post.",
    "As an author, I want deleting a post to redirect me to the index page so that I return to the post list after deletion."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/test_db.py": [
    "As a developer, I want get_db to return the same database connection when called multiple times within the same application context so that code reuses a single connection and avoids creating redundant connections.",
    "As a developer, I want the database connection to be closed when the application context ends so that resources are released and subsequent operations cannot be performed on a closed connection.",
    "As a developer, I want attempts to use the database after the connection is closed to raise a sqlite3.ProgrammingError that indicates the connection is closed so that failures are explicit and diagnosable.",
    "As an administrator, I want the init-db CLI command to invoke the application's database initialization routine and output a confirmation message (e.g., 'Initialized') so that the database is properly initialized and the user receives clear feedback."
  ],
  "/Users/richardlui/School/groundwork/flask/examples/tutorial/tests/test_factory.py": [
    "As a developer, I want the application to default to non-testing mode when no test configuration is provided so that production behavior is preserved and tests do not accidentally run in testing mode.",
    "As a developer, I want the application to respect a provided TESTING configuration so that I can enable testing mode when running tests.",
    "As a user, I want a GET request to /hello to return \"Hello, World!\" so that I receive a consistent greeting response from the endpoint."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/app.py": [
    "As a developer, I want the application to convert integer or None lifetime values into timedelta objects so that session and cache lifetimes are normalized and handled consistently.",
    "As a developer, I want the router to require unique endpoint names when adding URL rules so that endpoint collisions and ambiguous URL building are prevented.",
    "As a developer, I want the system to automatically provide an OPTIONS response for routes when appropriate so that HTTP clients (including CORS preflight requests) receive the correct allowed-methods information.",
    "As a developer, I want undefined HTTP methods for a route to default appropriately (e.g., GET when not specified) so that simple view functions work without verbose configuration.",
    "As a developer, I want HEAD requests to be handled automatically by the GET view logic when a separate HEAD handler is not provided so that clients can retrieve headers without extra code.",
    "As a developer, I want url building (url_for) to raise a BuildError when an endpoint is missing or required values are not provided so that URL generation failures are explicit and diagnosable.",
    "As a developer, I want static files to be served only from the configured static folder with path safety checks so that files outside the intended directory cannot be exposed.",
    "As a developer, I want the app to convert various return types (strings, tuples, Response objects, etc.) into a proper Response object so that view functions can return flexible types and the framework handles normalization.",
    "As a developer, I want a default secure session interface (cookie-based signed sessions) to be used when no session interface is configured so that session data is persisted securely by default.",
    "As a developer, I want error handlers to be registrable by exception type or HTTP status code so that custom responses or recovery can be provided for specific errors.",
    "As a developer, I want uncaught exceptions during request handling to be passed to the registered exception handlers (and fall back to an internal server error) so that error reporting and recovery are centralized and safe.",
    "As a developer, I want before_request functions to run before request dispatch and teardown_request functions to run after the response (even if an exception occurred) so that setup and cleanup work reliably around each request.",
    "As a developer, I want after_request functions to be able to modify the Response before it is sent so that response-level post-processing can consistently occur.",
    "As a developer, I want the application context and request context to be pushed when handling requests and popped during teardown so that globals like request, session, and g are available only within the correct scope.",
    "As a developer, I want template filters, globals, and tests to be registerable on the application's Jinja environment so that templates can be extended with custom behavior.",
    "As a developer, I want the Flask CLI integration to optionally load environment variables from a .env file and register commands so that development and management tasks are discoverable and can run with the app's configuration.",
    "As a developer, I want run() to enable the reloader and debugger when debug mode is enabled and rerun the process as needed so that code changes are picked up during development automatically.",
    "As a developer, I want make_response to accept and normalize many value types and allow explicit status and headers in return tuples so that view authors can control response status and headers conveniently."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/blueprints.py": [
    "As a developer, I want the Blueprint to create a Click command group (AppGroup) so that CLI commands for the blueprint can be registered and exposed via the application's CLI.",
    "As a developer, I want the Blueprint's CLI group to be named after the blueprint's name so that the blueprint's commands can be identified and added to other CLI tools consistently.",
    "As a developer, I want blueprint CLI commands to become available only after the application has been discovered and blueprints have been registered so that commands are registered in the correct application context.",
    "As a developer, I want get_send_file_max_age to read SEND_FILE_MAX_AGE_DEFAULT from the current application's configuration so that file caching behavior follows the application's configured policy.",
    "As a developer, I want get_send_file_max_age to return None when the configured SEND_FILE_MAX_AGE_DEFAULT is None so that browsers use conditional requests instead of timed caching when preferred.",
    "As a developer, I want get_send_file_max_age to convert a timedelta configuration to an integer number of seconds so that the max_age value returned is a numeric seconds value suitable for caching headers.",
    "As a developer, I want get_send_file_max_age to return numeric configuration values unchanged so that explicitly configured numeric cache durations are respected."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/cli.py": [
    "As a developer, I want the CLI to check a module for attributes named 'app' or 'application' and return the Flask instance if found so that common naming conventions are automatically supported.",
    "As a developer, I want the CLI to return the single Flask instance found in a module when exactly one exists so that a module with only one app is discovered without extra configuration.",
    "As a developer, I want the CLI to raise a clear usage error when multiple Flask instances are detected in a module and tell me how to specify the correct one so that ambiguity is resolved and the correct application is loaded.",
    "As a developer, I want the CLI to look for factory functions named 'create_app' or 'make_app', call them with no arguments, and use the returned Flask instance so that app factories are supported by default.",
    "As a developer, I want the CLI to detect when an app factory cannot be called without arguments (TypeError) and treat that as a non-automatic loadable factory, prompting me to specify the app explicitly so that factories that require parameters do not cause confusing failures.",
    "As a developer, I want the CLI to accept explicit module specifiers like 'module:name' and import the named object so that I can point the CLI at a specific application object or factory.",
    "As a developer, I want the CLI to raise a NoAppException (a usage error) when it cannot find or load an application so that I receive clear feedback and guidance instead of obscure failures.",
    "As a developer, I want blueprint configuration strings to be validated and split into key/value pairs when provided so that blueprint settings are parsed correctly and invalid formats are rejected with an error.",
    "As a developer, I want the CLI to respect environment-driven flags such as the debug flag and dotenv loading so that running the app picks up the appropriate debug behavior and environment variables.",
    "As a developer, I want the CLI to detect when the server is running under the Werkzeug reloader and adjust its behavior accordingly so that the development server functions correctly in reloader mode."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/config.py": [
    "As a store owner, I want attribute descriptors to forward reads to the central config and apply an optional converter so that accessing configuration values from the app returns the configured and correctly-typed value.",
    "As a store owner, I want setting a ConfigAttribute on the app to write into the config dictionary under the attribute name so that updates to attributes persist in the central config.",
    "As a store owner, I want the configuration object to behave like a dict but provide methods to populate it from files or special dictionaries so that I can manage configuration from multiple sources in a consistent way.",
    "As a store owner, I want to load configuration from a Python file using from_pyfile so that I can keep settings in external files and load them into the app.",
    "As a store owner, I want to load configuration from a module or import path using from_object so that I can define configuration values in code and reuse or share configuration modules.",
    "As a store owner, I want to be able to use the same module that calls from_object and set configuration values just before the call so that I can define configuration inline in the calling module.",
    "As a store owner, I want the system to add only uppercase keys when loading configuration from Python files or modules so that temporary or internal lowercase values are ignored and only intended settings are applied.",
    "As a store owner, I want to load configuration from an environment variable pointing to a file using from_envvar so that deployment or runtime environments can control which configuration file is used.",
    "As a store owner, I want the environment variable referenced by from_envvar to be set before launching the application so that the application can locate and load the configuration file at startup."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/ctx.py": [
    "As a developer, I want a per-application namespace object (g) that supports attribute get/set/del and raises AttributeError for missing names so that I can store arbitrary contextual data and get clear failures for missing attributes.",
    "As a developer, I want application contexts to be created with a reference to the Flask app and a fresh g object so that application-scoped resources are available while the context is active.",
    "As a developer, I want pushing an application context to emit the appcontext_pushed signal so that extension and app code can run setup when an app context becomes active.",
    "As a developer, I want popping an application context to emit the appcontext_popped signal and run all teardown callbacks so that listeners and cleanup logic run when the context is removed.",
    "As a developer, I want teardown callbacks invoked during app context/pop or request context/pop to have their exceptions collected and re-raised after all callbacks run so that all cleanup is attempted and errors are not lost.",
    "As a developer, I want the request context to bind the application's URL map to the WSGI environment and perform URL matching so that the request's endpoint, view_args, and url_rule are populated for routing.",
    "As a developer, I want URL matching errors that are HTTPException instances to be stored on the request (routing_exception) instead of being swallowed so that the request handling pipeline can detect and respond to routing failures.",
    "As a developer, I want pushing a request context to implicitly push the corresponding application context if it is not already present so that app-level resources are available during request processing.",
    "As a developer, I want the request and application contexts to manage a top-of-stack context using context variables so that code can reliably access the currently active context even in asynchronous environments.",
    "As a developer, I want the request context push/pop sequence to run request teardown handlers and session cleanup when popped so that per-request resources are properly finalized.",
    "As a developer, I want the request context to implement the context manager protocol (__enter__/__exit__) so that request contexts can be used with with-statements to ensure deterministic push/pop behavior.",
    "As a developer, I want the url_adapter to be created lazily (bound to environ) and cached on the context so that URL matching is efficient and uses the correct environment binding.",
    "As a developer, I want g to provide dict-like helpers (get with default, pop with sentinel/default) so that developers can interact with the context namespace in a familiar way.",
    "As a developer, I want any exceptions raised during teardown to be surfaced after all teardown handlers have run so that debugging information is available and teardown is not partially applied.",
    "As a developer, I want context management to use contextvars so that separate asynchronous execution contexts do not interfere with each other's app/request contexts."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/debughelpers.py": [
    "As a developer, I want the system to raise a DebugFilesKeyError when I try to access a file in request.files but the request mimetype is not 'multipart/form-data' so that I receive a clear explanation to add enctype=\"multipart/form-data\" to my form and understand why no file contents were transmitted.",
    "As a developer, I want the DebugFilesKeyError message to include any file names that the browser transmitted in request.form so that I can diagnose cases where only filenames (and not file contents) were submitted by the client.",
    "As a developer, I want the system to raise a FormDataRoutingRedirect in debug mode when a routing redirect would change the request method or drop the request body (i.e., for non-GET/HEAD/OPTIONS requests redirected with status not 307/308) so that I am alerted to use the canonical URL or use 307/308 redirects to preserve form data.",
    "As a developer, I want the FormDataRoutingRedirect to indicate when the redirect is caused by a missing or extra trailing slash so that I can correct URL definitions or client requests to avoid unintended redirects.",
    "As a developer, I want unexpected Unicode or binary data to raise an UnexpectedUnicodeError so that I get more informative error reporting for encoding-related issues."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/globals.py": [
    "As a developer, I want the system to require an application context before allowing access to application-bound globals (current_app, app_ctx, g) so that I receive a clear error message and instructions to set up an app context (for example using with app.app_context()).",
    "As a developer, I want the system to require a request context before allowing access to request-bound objects (request, session) so that I receive a clear error message and instructions to use a request context when performing HTTP-specific operations.",
    "As a developer, I want current_app to act as a proxy for the active application's 'app' attribute from the current app context so that I can access the current Flask application object transparently from any code that has the proper context.",
    "As a developer, I want g to act as a proxy for the active app context's 'g' attribute so that I can reliably store and retrieve application/request-scoped globals within the active context.",
    "As a developer, I want request to act as a proxy for the active app context's 'request' attribute so that I can access the current HTTP request object from anywhere within the request context.",
    "As a developer, I want session to act as a proxy for the active app context's 'session' attribute so that I can access session data tied to the current request within the proper context.",
    "As a developer, I want accessing the deprecated name 'request_ctx' to emit a DeprecationWarning and return app_ctx so that existing code continues to work while being guided to migrate to 'app_ctx' before removal in a future release.",
    "As a developer, I want context-bound proxies to use a ContextVar as their underlying storage so that context resolution is isolated and consistent across concurrent execution contexts."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/helpers.py": [
    "As a developer, I want the system to enable debug mode when the FLASK_DEBUG environment variable is set to a truthy value (not '0', 'false', or 'no') so that debugging behavior can be turned on via environment configuration.",
    "As a developer, I want the system to load dotenv files by default unless FLASK_SKIP_DOTENV is set to a value that indicates skipping (values other than '0', 'false', or 'no' mean skip) so that environment variables are loaded only when desired.",
    "As a developer, I want generator-based responses to be executed inside the original request context so that request, session, and context-local data remain available while the generator yields.",
    "As a developer, I want the system to ensure all response headers are set before a streaming response generator starts yielding so that headers cannot be modified after the body has begun and header-related errors are avoided.",
    "As a developer, I want the system to ensure that if a streaming generator will access the session, the session is accessed early (e.g., in the view) so that appropriate headers like 'Vary: cookie' are set before the response body is sent.",
    "As a developer, I want the helper that joins filesystem paths to reject or fail when the resulting path would escape the intended directory so that path traversal attacks are prevented and files outside the target directory are not served.",
    "As a developer, I want send_file and similar file-serving helpers to support conditional requests (e.g., If-Modified-Since / ETag) so that the server can return 304 Not Modified when appropriate and save bandwidth.",
    "As a developer, I want make_response to accept multiple types of return values (strings, bytes, iterables, Response objects, and tuples with status/headers) and normalize them into a proper Response so that view functions can return flexible result types.",
    "As a developer, I want flash() to store messages in the session under a category (defaulting to a generic category) and emit a message_flashed signal so that messages persist across requests and other components can react to them.",
    "As a developer, I want get_flashed_messages() to retrieve flashed messages from the session, optionally include categories, and remove them once retrieved so that messages are shown to the user only once and can be filtered by category.",
    "As a developer, I want a utility to locate a package or module's filesystem path (raising an error if not found) so that application code can reliably determine an application's root path for loading resources.",
    "As a developer, I want a cached property decorator that is thread-safe (locks during first computation) so that expensive per-instance computations are only done once and are safe under concurrent access.",
    "As a developer, I want the application root path resolution logic to prefer package module information when available and raise clear errors when a root cannot be determined so that application setup fails fast and predictably when misconfigured."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/json/__init__.py": [
    "As a developer, I want JSON serialization functions (dumps/dump) to use the application's JSON provider when an application context is available so that app-specific serialization behavior and configuration are applied.",
    "As a developer, I want JSON serialization functions to fall back to Python's built-in json module when no application context is available so that serialization still works outside of an app context.",
    "As a developer, I want the serialization functions to set a default serializer for unsupported types when none is provided so that types like decimal.Decimal and other non-JSON-native objects can be converted (e.g., to strings) instead of causing errors.",
    "As a developer, I want the json.dump function to write to a text file opened with UTF-8 encoding so that the produced output is valid JSON and uses a consistent text encoding.",
    "As a developer, I want JSON deserialization functions (loads/load) to use the application's JSON provider when an application context is available so that custom deserialization logic and configuration are honored.",
    "As a developer, I want loads to accept both str and bytes inputs so that the system can deserialize JSON from different input types.",
    "As a developer, I want htmlsafe_dumps to ensure ASCII output by default and to escape characters like '<', '>', '&' and the Unicode line/paragraph separators (U+2028, U+2029) so that serialized JSON can be safely embedded in HTML and reduce the risk of XSS.",
    "As a developer, I want htmlsafe_dumps to delegate to the application's JSON provider when an application context exists so that any app-level HTML-safe serialization behavior is applied consistently.",
    "As a developer, I want the module to only set fallback behaviors (like default serializer and ensure_ascii defaults) when the application provider is not available so that explicit app configuration is not overridden."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/json/provider.py": [
    "As a store owner, I want the JSON provider to hold a weak reference to the application so that it does not create strong reference cycles and allows the app to be garbage collected.",
    "As a store owner, I want the provider to require subclasses to implement dumps and loads so that concrete providers must supply the actual serialization and deserialization behavior.",
    "As a store owner, I want dump to write JSON text to a file by delegating to dumps so that file output is consistent with the provider's serialization logic.",
    "As a store owner, I want load to read JSON text or UTF-8 bytes from a file by delegating to loads so that file input is deserialized consistently with the provider's parsing logic.",
    "As a customer, I want the response builder to accept either positional arguments or keyword arguments but not both so that the produced JSON body is unambiguous and predictable.",
    "As a customer, I want multiple positional arguments passed to the response builder to be represented as a JSON array so that callers can easily return a list of values.",
    "As a customer, I want a single positional argument passed to the response builder to be serialized as that value so that simple responses are returned without extra nesting.",
    "As a customer, I want keyword arguments passed to the response builder to be serialized as a JSON object so that named data is returned as an object with properties.",
    "As a store owner, I want the default serializer to convert dataclass instances into dictionaries so that dataclasses become JSON serializable without custom code.",
    "As a store owner, I want the default serializer to convert types that are not natively JSON serializable (like UUIDs, dates, decimals, sets, and bytes) into JSON-compatible types so that common Python types can be returned in responses.",
    "As a store owner, I want dumps and loads to accept and forward keyword arguments to the underlying JSON library so that callers can customize serialization and deserialization behavior.",
    "As a store owner, I want the system to raise a clear TypeError when app.json.response() is called with both args and kwargs so that developers get immediate feedback about incorrect usage.",
    "As a store owner, I want the response creation path to allow callers to provide status and headers (when supported) so that responses can include appropriate HTTP status codes and additional headers alongside the JSON body."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/json/tag.py": [
    "As a developer, I want non-standard Python types to be converted into JSON-valid representations with an associated tag so that they can be serialized and deserialized losslessly.",
    "As a developer, I want to implement JSONTag subclasses that provide check, to_json, and to_python methods so that the serializer can detect, convert, and reconstruct custom types.",
    "As a developer, I want a tag's key attribute to identify the serialized type and allow an empty key to indicate an intermediate-only tag so that only intended types produce persistent tags in the JSON output.",
    "As a developer, I want to be able to register custom tags at a specific index/order so that specialized types (for example OrderedDict) can be processed before their more general base types (for example dict) and preserve semantics like ordering.",
    "As a developer, I want the tag.to_json method to return only valid JSON types so that the JSON encoder can successfully serialize the result.",
    "As a developer, I want the serializer to recursively tag nested structures (list items, dict values, etc.) so that complex objects containing non-standard types are fully and correctly serialized.",
    "As a developer, I want bytes objects to be serialized as Base64-encoded strings and decoded back to bytes on load so that arbitrary binary data can be safely represented in JSON.",
    "As a developer, I want UUID objects to be serialized to their string representation and reconstructed as UUID objects on load so that unique identifiers are preserved across serialization.",
    "As a developer, I want datetime objects to be serialized as HTTP-date strings using http_date and parsed back to datetime objects using parse_date so that date/time values are interoperable and preserved.",
    "As a developer, I want Markup (HTML-safe) objects to be serialized as strings and reconstructed as Markup on deserialization so that HTML-safety semantics are preserved.",
    "As a developer, I want set and frozenset objects to be converted to a JSON-safe representation (for example a tagged list) and reconstructed as the original set type on load so that non-ordered collections can be round-tripped.",
    "As a developer, I want tuple objects (when treated specially) to be serialized with an explicit tag if needed so that tuple semantics can be preserved when JSON would otherwise represent them as lists.",
    "As a developer, I want the session serializer to use the tagged JSON serializer so that session data containing non-standard types is stored and restored reliably.",
    "As a developer, I want the tagging machinery to ensure that dict-like types and plain dicts are handled in the correct order to avoid losing specialized type information during serialization.",
    "As a developer, I want the deserialization process to validate and convert tagged payloads back to the expected Python types (and raise or handle errors on malformed payloads) so that corrupted or tampered data does not silently produce incorrect objects."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/logging.py": [
    "As an app developer, I want the logging system to write to request.environ['wsgi.errors'] when a request context is active so that error logs are associated with the current request environment.",
    "As an app developer, I want the logging system to fall back to sys.stderr when no request context is active so that logs are still captured outside of request handling.",
    "As an app developer, I want the logger name to be the application's import name so that log records are identifiable by application.",
    "As an app developer, I want the logger's level to be set to DEBUG automatically when the application debug mode is enabled and the logger has no explicit level so that detailed debug output is available during development.",
    "As an app developer, I want the system to check the logger chain for any handler that can handle the logger's effective level (respecting handler.level and propagate flags) so that duplicate or unnecessary handlers are not added.",
    "As an app developer, I want a default StreamHandler using the wsgi_errors_stream to be added when no handler will handle the logger's effective level so that log messages are emitted consistently.",
    "As an app developer, I want the default log messages to use the format \"[%(asctime)s] %(levelname)s in %(module)s: %(message)s\" so that log entries have a consistent, informative structure.",
    "As an app developer, I want to be able to reference the wsgi_errors_stream externally (e.g. ext://flask.logging.wsgi_errors_stream) when using file or dict logging configuration so that custom handlers can target the appropriate error stream."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/sansio/app.py": [
    "As an app developer, I want the system to accept timeout values as either a timedelta or an integer and convert integers to timedelta so that time-based configuration is normalized and handled consistently.",
    "As an app developer, I want the application to resolve resources relative to the provided module or package name so that files and templates inside the application package can be reliably located.",
    "As an app developer, I want the application to act as a central registry for view functions, URL rules, and template configuration so that routing and rendering are consistently managed from a single place.",
    "As an app developer, I want URL rules and view functions to be registered with unique endpoints (derived from the view function when appropriate) so that route dispatching can locate the correct handler unambiguously.",
    "As an app developer, I want blueprint paths to be split and applied when registering blueprint routes so that endpoints and URL structures from blueprints are consistently namespaced and resolvable.",
    "As an app developer, I want the system to raise a BuildError when URL generation fails so that callers are informed when a URL cannot be constructed.",
    "As an app developer, I want the default JSON provider to be used when no custom provider is configured so that JSON serialization and deserialization behave predictably out of the box.",
    "As an app developer, I want the debug flag to be determined from configuration or environment using get_debug_flag so that the application can toggle debug behavior based on deployment settings.",
    "As an app developer, I want logging to be created/configured from the application's configuration so that logging behavior (levels, handlers) aligns with application settings.",
    "As an app developer, I want an abort facility (Aborter) available to raise HTTP errors programmatically so that request handling can stop with appropriate HTTP responses when needed.",
    "As an app developer, I want the template loader to dispatch across the application and registered blueprints so that template lookup respects both app-level and blueprint-provided templates.",
    "As an app developer, I want invalid request data or missing required keys to result in BadRequest or BadRequestKeyError exceptions so that callers receive clear validation failure responses.",
    "As an app developer, I want response objects to be created consistently through the application layer (wrapping werkzeug response behavior) so that handlers can return various return types and get a valid Response.",
    "As an app developer, I want URL rule additions and routing configuration to be centralized and validated when added so that misconfigured routes are detected early and routing remains consistent."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/sansio/blueprints.py": [
    "As a developer, I want the blueprint registration to use the 'subdomain' option if provided and fall back to the blueprint's configured subdomain so that registrations can override where the blueprint is active without changing the blueprint itself.",
    "As a developer, I want the blueprint registration to use the 'url_prefix' option if provided and fall back to the blueprint's configured url_prefix so that I can mount the same blueprint at different URL prefixes per registration.",
    "As a developer, I want the setup state name to come from the registration option 'name' if provided and otherwise use the blueprint's name so that I can register the same blueprint under a different logical name.",
    "As a developer, I want the setup state to include a 'name_prefix' option defaulting to empty so that I can add a prefix to all endpoint names on registration to avoid endpoint name collisions.",
    "As a developer, I want the framework to provide a 'first_registration' flag to setup functions so that code can choose to perform one-time setup only on the first registration and skip duplicate work on subsequent registrations.",
    "As a developer, I want deferred setup callbacks recorded via the blueprint to be executed during registration so that blueprints can programmatically add routes, handlers, and other configuration when they are attached to an application.",
    "As a developer, I want a distinction between callbacks that run on every registration and callbacks that run only once so that initialization that must be idempotent or expensive can be controlled.",
    "As a developer, I want endpoints defined by a blueprint to have their endpoint names prefixed with the configured name_prefix and blueprint name so that endpoints from different blueprints or registrations do not conflict.",
    "As a developer, I want the blueprint's url_prefix to be prepended to all URL rules added by the blueprint so that routes defined in the blueprint are correctly namespaced under the mounted prefix.",
    "As a developer, I want blueprint-registered before_request functions to execute for requests that match blueprint routes so that per-blueprint request preparation can occur.",
    "As a developer, I want blueprint-registered after_request functions to execute for responses from blueprint routes so that per-blueprint response processing can occur.",
    "As a developer, I want blueprint-registered teardown functions to run when the request context ends for blueprint-handled requests so that resources opened by blueprint logic can be cleaned up.",
    "As a developer, I want blueprint-registered error handlers to be associated with the blueprint so that errors raised while handling blueprint endpoints can be handled by blueprint-specific logic.",
    "As a developer, I want template filters, globals, tests, and context processors defined on a blueprint to be registered on the application when the blueprint is registered so that templates used by the blueprint have access to those utilities.",
    "As a developer, I want URL default functions and URL value preprocessors defined on a blueprint to be applied to the blueprint's endpoints so that URL generation and URL matching can include blueprint-specific logic.",
    "As a developer, I want blueprint static folders and static URL paths to result in a static route when the blueprint is registered so that blueprint-local static files are served under the appropriate URL namespace."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/sansio/scaffold.py": [
    "As a developer, I want the system to prevent calling setup methods after setup is finished so that the application's configuration cannot be modified at runtime.",
    "As a developer, I want the scaffold to determine the application's root path from the import name when root_path is not provided so that static, template, and resource locations are resolved automatically.",
    "As a developer, I want the scaffold to add a static route automatically when a static_folder is provided so that static files are served without manual route creation.",
    "As a developer, I want the scaffold to set a static URL prefix when static_url_path is provided so that static files are accessible at the expected URL.",
    "As a developer, I want the scaffold to add a Jinja2 FileSystemLoader when a template_folder is provided so that templates can be loaded from the specified folder.",
    "As a developer, I want the system to treat a special sentinel value as the absence of an explicit parameter so that the code can distinguish between omitted arguments and explicit None defaults.",
    "As a developer, I want default template context processors to be available so that templates have common context variables without extra setup.",
    "As a developer, I want wrapper functions created by setupmethod to preserve the original function's metadata so that debugging and introspection remain accurate."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/sessions.py": [
    "As a customer, I want the system to persist a '_permanent' flag in my session so that selecting a remember-me behavior causes my session to be treated as permanent.",
    "As a store owner, I want the system to only write a session cookie when the session is marked as modified so that unchanged sessions do not cause unnecessary cookie updates.",
    "As a store owner, I want the system to require manual marking of the session as modified when mutating nested mutable objects so that in-place changes are not silently lost.",
    "As a store owner, I want the system to track when a session was accessed so that responses can include a Vary: Cookie header when appropriate.",
    "As a customer, I want permanent sessions to use the application's configured permanent_session_lifetime for cookie expiration so that persistent sessions expire according to policy.",
    "As a store owner, I want session signing and verification to require a configured secret key and serializer so that session cookies cannot be forged or tampered with.",
    "As a customer, I want invalid or tampered session cookies to be ignored and a new empty session created so that corrupted cookies do not grant access or crash the application.",
    "As a customer, I want expired session cookies to be treated as invalid and result in a new session so that stale session data is not reused.",
    "As a store owner, I want the system not to set a session cookie when the session is empty and not modified so that unnecessary cookies are not sent to clients.",
    "As a store owner, I want the system to delete the session cookie on the client when the session is emptied or explicitly cleared so that client state is synchronized with server state.",
    "As a store owner, I want the session cookie name and attributes (domain, path, secure, httponly, samesite) to be configurable via application settings so that cookie behavior can be adapted to deployment needs.",
    "As a store owner, I want session data to be serialized with a JSON-based tagged serializer and signed with a time-aware serializer so that complex objects are stored safely and sessions can be validated and aged."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/signals.py": [
    "As an application developer, I want the framework to provide a dedicated signals namespace for framework-provided signals so that my application's signals do not conflict with Flask's internal signals.",
    "As an application developer, I want the system to emit a 'before-render-template' signal before rendering a template so that I can modify the template context or perform preparatory actions.",
    "As an application developer, I want the system to emit a 'template-rendered' signal after a template is rendered so that I can perform post-render actions such as logging or caching.",
    "As an application developer, I want the system to emit a 'request-started' signal when a request begins so that I can initialize request-scoped resources or instrumentation.",
    "As an application developer, I want the system to emit a 'request-finished' signal when a request completes successfully so that I can finalize resources, logging, or metrics.",
    "As an application developer, I want the system to emit a 'request-tearing-down' signal when tearing down a request so that I can reliably clean up resources regardless of outcome.",
    "As an application developer, I want the system to emit a 'got-request-exception' signal when a request raises an exception so that I can handle error reporting, logging, or custom recovery workflows.",
    "As an application developer, I want the system to emit an 'appcontext-pushed' signal when an application context is pushed so that I can set up context-specific state or resources.",
    "As an application developer, I want the system to emit an 'appcontext-popped' signal when an application context is popped so that I can tear down context-specific state or release resources.",
    "As an application developer, I want the system to emit an 'appcontext-tearing-down' signal when an application context is being torn down so that cleanup tasks always run at context shutdown.",
    "As an application developer, I want the system to emit a 'message-flashed' signal when a flash message is added so that I can audit, transform, or react to user-facing notifications."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/templating.py": [
    "As an app developer, I want the template context processor to provide the concrete 'g' object so that templates can access application globals efficiently.",
    "As an app developer, I want the template context processor to include the concrete 'request' object when a request context exists so that templates can access request data quickly without proxy overhead.",
    "As an app developer, I want the session object to remain a proxy rather than being replaced in the template context so that accessing the session continues to mark it as accessed (session.accessed).",
    "As an app developer, I want the Jinja Environment to default to app.create_global_jinja_loader() when no loader is provided so that the application-level template loading behavior is used automatically.",
    "As a template author, I want the environment to be aware of blueprints and be able to prepend blueprint names to referenced templates when necessary so that blueprint-local templates can be referenced unambiguously.",
    "As an app developer, I want the dispatching Jinja loader to search the application and all blueprint template folders so that templates can be located across the app and its blueprints.",
    "As an app developer, I want the dispatching Jinja loader to collect and present detailed loading attempts when EXPLAIN_TEMPLATE_LOADING is enabled so that I can debug why a template was not found.",
    "As an app developer, I want the loader to try each configured loader in turn and return the first successful template source (or raise TemplateNotFound if none match) so that template resolution is predictable and failures are reported clearly.",
    "As an app developer, I want template sources to provide their source, filename (or None), and an 'uptodate' callable so that template caching and reload checks function correctly."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/testing.py": [
    "As a developer, I want the request builder to reject configurations that supply a base_url together with a subdomain or url_scheme so that ambiguous or conflicting URL configuration cannot be passed to the builder.",
    "As a developer, I want the EnvironBuilder to construct a base URL from the application's SERVER_NAME, APPLICATION_ROOT and PREFERRED_URL_SCHEME (and an optional subdomain) when no explicit base_url is provided so that test requests reflect the application's configured host, root path, and scheme.",
    "As a developer, I want the EnvironBuilder to parse a provided path with urlsplit and use the path component for the request while using the scheme and netloc components to form the base_url so that full URLs passed as the path are handled correctly.",
    "As a developer, I want the EnvironBuilder to default the host to the application's SERVER_NAME or to 'localhost' when no host is determinable so that requests always have a sensible host value in tests.",
    "As a developer, I want the EnvironBuilder to use the application's PREFERRED_URL_SCHEME when no url_scheme is supplied so that requests default to the app's preferred scheme.",
    "As a tester, I want the test client to accept a json parameter and automatically serialize it into the request body and default the content_type to application/json so that JSON test requests are correctly formed without manual serialization.",
    "As a tester, I want a session_transaction context manager that allows me to open and modify the session during tests so that I can set up or inspect session state reliably within test scopes.",
    "As a tester, I want the test client to follow redirects when follow_redirects is enabled and to perform subsequent requests until a non-redirect response or an enforced limit is reached so that I can validate end-to-end redirect flows and final responses."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/typing.py": [
    "As a developer, I want route handlers to be able to return Response objects, strings, bytes, lists, mappings, iterators, and async iterables so that the framework can convert a wide range of return types into HTTP responses.",
    "As a developer, I want mappings to be accepted where dicts are expected so that TypedDict and other mapping-like structures can be used for JSON-like responses.",
    "As a developer, I want header values to be accepted as a single string, a list of strings, or a tuple of strings so that multiple header formats are supported.",
    "As a developer, I want headers to be accepted as a Headers object, a mapping of header names to header values, or a sequence of (name, value) pairs so that different header representations are interoperable.",
    "As a developer, I want route functions to be able to return a ResponseValue alone, a ResponseValue with headers, a ResponseValue with a status code, a ResponseValue with a status code and headers, or a WSGI application so that handlers can specify status and headers flexibly or delegate to WSGI apps.",
    "As a developer, I want callback annotations to allow any subclass of werkzeug.Response so that custom Response subclasses used by Flask or extensions type-check correctly.",
    "As a developer, I want the application key to be None and blueprint keys to be strings so that the system can distinguish app-level handlers from named blueprints.",
    "As a developer, I want after-request callables to accept a Response (or subclass) and return a Response (or an awaitable Response) so that response post-processing can be synchronous or asynchronous and must produce a Response.",
    "As a developer, I want before-first-request callables to take no arguments and return None (or an awaitable None) so that initialization logic can run once before the first request without producing a response.",
    "As a developer, I want before-request callables to be able to return a ResponseReturnValue or None (or an awaitable of those) so that they can abort a request by returning a response or continue by returning None.",
    "As a developer, I want shell context processors to return a dictionary of names to values so that the interactive shell receives a predictable context mapping.",
    "As a developer, I want teardown callables to accept an optional BaseException and return None (or an awaitable None) so that cleanup logic can handle errors and always returns nothing.",
    "As a developer, I want template context processors to return a dict (or an awaitable dict) so that templates receive consistent context data.",
    "As a developer, I want template filters and globals to be defined as callables returning any value so that custom template helpers can produce arbitrary results.",
    "As a developer, I want template tests to be callables that return a boolean so that template test expressions evaluate to true/false.",
    "As a developer, I want URL default functions to receive an endpoint and the URL values mapping and return None so that they can supply default values without producing a return value.",
    "As a developer, I want URL value preprocessor callables to receive an optional endpoint and optional values mapping and return None so that they can modify URL values before routing without returning a value.",
    "As a developer, I want error handler callables to accept any type of exception and return a ResponseReturnValue so that handlers can handle different exception types and produce appropriate responses."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/views.py": [
    "As a developer, I want to subclass View and override dispatch_request so that each view can implement the actual response logic for incoming requests.",
    "As a developer, I want the system to provide a class method as_view that produces a callable view function so that my class-based views can be registered with the routing system.",
    "As a developer, I want the generated view function to instantiate the view class for each request when init_every_request is True so that per-request state is isolated and safe.",
    "As a developer, I want the generated view function to reuse a single view instance when init_every_request is False so that expensive initialization can be avoided and performance improved.",
    "As a developer, I want the view class to allow specifying allowed HTTP methods via the methods attribute so that only intended HTTP methods are accepted for the endpoint.",
    "As a developer, I want the system to recognize and use a standard set of HTTP method names (get, post, put, delete, patch, head, options, trace) so that method handling is consistent and validated.",
    "As a developer, I want HEAD to be supported automatically when GET is allowed and provide_automatic_options to control automatic OPTIONS handling so that common HTTP behaviors are provided without extra code.",
    "As a developer, I want the generated view function to have metadata attributes (such as __name__, view_class, methods, and provide_automatic_options) so that the routing system and tooling can introspect and manage the view correctly.",
    "As a developer, I want to be able to attach a list of decorators on the view class that are applied, in order, to the generated view function so that cross-cutting behaviors (authentication, caching, etc.) can be composed declaratively.",
    "As a developer, I want the dispatch_request method to be called with any URL variables so that my view can receive path parameters from route matching.",
    "As a developer, I want the system to document that storing request-global data on self is unsafe when reusing a single view instance so that developers use the request-local storage (e.g., flask.g) and avoid data leaks between requests.",
    "As a developer, I want the system to provide sensible defaults for methods and provide_automatic_options when not explicitly set on the view class so that simple views work out-of-the-box without additional configuration."
  ],
  "/Users/richardlui/School/groundwork/flask/src/flask/wrappers.py": [
    "As a developer, I want the system to enforce a per-request max_content_length (falling back to the application's MAX_CONTENT_LENGTH) so that excessively large request bodies are rejected and a 413 Request Entity Too Large error is raised.",
    "As a developer, I want a request's max_content_length to be overrideable on a per-request basis so that specific views can impose stricter or looser upload limits than the global config.",
    "As a developer, I want the system to avoid reading the request body when max_content_length is None and there is no Content-Length header and the server does not terminate the stream so that the application does not hang on an infinite stream.",
    "As a developer, I want the request to enforce a max_form_memory_size (with a default from app config) so that form parsing does not consume more memory than allowed.",
    "As a developer, I want the request to enforce a max_form_parts limit so that multipart form submissions with too many parts are rejected to protect against resource exhaustion.",
    "As a developer, I want request.get_json (and request.json) to only parse JSON when the content type indicates JSON or when force=True, so that non-JSON requests do not cause unexpected parsing attempts.",
    "As a developer, I want request.get_json to support a silent flag that suppresses exceptions and returns None on malformed or missing JSON when enabled, so that callers can opt into lenient parsing behavior.",
    "As a developer, I want request.get_json to support a cache option (and cache by default for request.json) so that repeated accesses to the parsed JSON reuse the result instead of reparsing the body.",
    "As a developer, I want the application to call on_json_loading_failed when JSON decoding fails and, by default, raise a BadRequest so that invalid JSON is consistently treated as a client error but can be customized by subclasses.",
    "As a developer, I want the Request object to expose url_rule, view_args, and routing_exception so that before/after handlers and views can inspect routing decisions, allowed methods, and routing errors.",
    "As a developer, I want the valid methods for a URL to be available from routing_exception.valid_methods when the request method is invalid for the matched rule so that handlers can determine and report allowed methods.",
    "As a developer, I want the response JSON handling to use the request/Flask-configured json_module so that JSON parsing and serialization can be customized or substituted consistently across requests and responses."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_appctx.py": [
    "As a Flask developer, I want url_for to generate fully qualified URLs using SERVER_NAME and PREFERRED_URL_SCHEME so that generated links include the correct host and scheme.",
    "As a Flask developer, I want url_for to raise a RuntimeError when SERVER_NAME is not configured so that URL generation does not proceed with ambiguous host information.",
    "As a Flask developer, I want url_for to require an active application context so that URL generation uses the correct application configuration and state.",
    "As a Flask developer, I want entering a request context to also make the application context available (current_app accessible) so that request handlers can access application state.",
    "As a Flask developer, I want current_app to be available only while an application or request context is active and be unavailable afterwards so that code cannot rely on application state outside managed contexts.",
    "As a Flask developer, I want teardown_appcontext handlers to be executed when an application context is popped so that application-level cleanup callbacks always run.",
    "As a Flask developer, I want teardown_appcontext handlers to receive the current exception (or None) as an argument so that cleanup logic can react to whether the context is exiting due to an error.",
    "As a Flask developer, I want teardown_appcontext handlers to still be called with None even if earlier exceptions were raised and handled before entering the context so that cleanup is consistently performed for normal exits.",
    "As a Flask developer, I want teardown_appcontext handlers to be executed in reverse order of registration so that resources are released in the correct dependency order.",
    "As a Flask developer, I want context manager helpers (like app.app_context() and app.test_request_context()) to manage pushing and popping contexts reliably so that tests and request handling code can depend on deterministic context lifecycle."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_async.py": [
    "As a developer, I want async route handlers to support GET and POST so that requests using those HTTP methods are handled asynchronously and return the correct method or response.",
    "As a developer, I want to be able to assign multiple URL routes to the same async view function so that the same handler can serve different paths without duplicating code.",
    "As a developer, I want to register async application-level error handlers for specific exception types so that exceptions raised in routes are converted to appropriate HTTP responses (for example, returning a 412 status for AppError).",
    "As a developer, I want to register async blueprint-level error handlers for specific exception types so that exceptions raised within a blueprint are handled by the blueprint's own handler (for example, returning a 412 status for BlueprintError).",
    "As a developer, I want to register a blueprint with a URL prefix so that all routes defined on the blueprint are exposed under a common path segment (e.g., /bp/*).",
    "As a developer, I want to create async class-based views (subclassing View) with a dispatch_request coroutine and an explicit methods list so that class-based endpoints can handle requests asynchronously and enforce allowed HTTP methods.",
    "As a developer, I want to create async MethodView subclasses that implement async get and post methods so that class-based method views can respond asynchronously with method-specific logic.",
    "As a developer, I want to register class-based views using add_url_rule with view_func equal to the view's as_view() so that the framework mounts the class-based view endpoints correctly and supports async handlers.",
    "As a developer, I want tests and async behavior to be skipped when required async support libraries (like asgiref) are not available so that the test suite does not fail when async dependencies are missing."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_basic.py": [
    "As a developer, I want the application to respond to OPTIONS requests with an Allow header listing allowed methods (including HEAD and OPTIONS) so that clients can discover which HTTP methods are supported for a URL.",
    "As a developer, I want the body of an automatic OPTIONS response to be empty so that method-discovery responses do not include a payload.",
    "As a developer, I want the Allow header for a URL to include the union of methods from all routes bound to that URL so that clients see all methods supported when multiple rules exist.",
    "As a developer, I want route decorators for specific HTTP methods (app.get, app.post, app.put, app.delete, app.patch) to register handlers that respond to the corresponding HTTP verbs so that requests to those verbs invoke the correct view.",
    "As a developer, I want method-specific route decorators to raise a TypeError if the 'methods' parameter is provided so that routes are not misconfigured by redundant method declarations.",
    "As a developer, I want HEAD to be automatically included in the allowed methods for endpoints that support GET so that clients can perform HEAD requests without an explicit HEAD route.",
    "As a developer, I want to be able to disable automatic OPTIONS handling by setting provide_automatic_options = False on a view function so that I can implement custom OPTIONS behavior or prevent automatic generation."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_blueprints.py": [
    "As a store owner, I want blueprint-specific HTTP error handlers to be invoked for errors raised in that blueprint's routes so that blueprint-level behavior can override global application error handling.",
    "As a store owner, I want the application-level HTTP error handler to be used when a blueprint does not define a specific handler so that all unhandled blueprint errors are still caught centrally.",
    "As a store owner, I want the system to allow registering exception handlers for a blueprint both via a decorator and via register_error_handler so that I can choose the preferred registration style and ensure blueprint exceptions are handled.",
    "As a store owner, I want exception handlers to receive the actual exception instance so that handlers can inspect exception details and make informed decisions about the response.",
    "As a store owner, I want calls to abort with an HTTP status inside a route to trigger the corresponding error handling flow so that errors raised by routes result in appropriate error responses."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_cli.py": [
    "As a developer, I want the CLI object's name to be set to the application's name so that the CLI displays a meaningful and stable identifier instead of an application object reference.",
    "As a developer, I want the system to locate the Flask app by checking common module attributes in order (app, application, then other attributes that are Flask instances) so that applications that expose their app under different attribute names are discovered automatically.",
    "As a developer, I want the system to detect and call a create_app factory (create_app() or create_app(**kwargs)) and return the resulting Flask instance so that applications using the app factory pattern are supported.",
    "As a developer, I want the system to raise a NoAppException with a clear message when it cannot locate or import an application from the given module or import path so that I get actionable feedback to fix the import or configuration.",
    "As a developer, I want commands decorated with with_appcontext to run inside an application context so that current_app and other context-bound resources are available during command execution.",
    "As a developer, I want load_dotenv to read .env files and set environment variables before the app is loaded so that configuration from .env files is available to CLI commands and the application at startup.",
    "As a developer, I want locate_app/run_command/ScriptInfo to support import strings that specify modules and factory callables (e.g., module:factory) so that I can reference apps defined by factories or in nested modules when running CLI commands.",
    "As a developer, I want the app discovery logic to handle create_app callables that accept optional keyword arguments so that factories with optional configuration parameters can still be invoked and produce an app instance."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_config.py": [
    "As a store owner, I want the application to load configuration values from a Python file so that secret keys and other settings defined in that file are applied to the app at startup.",
    "As a store owner, I want the application to load configuration values from a Python module/object so that I can package configuration as code and apply it to the app.",
    "As a store owner, I want the application to load configuration values from a JSON file (via a provided loader) so that structured settings can be provided in JSON and consumed by the app.",
    "As a store owner, I want the application to load configuration values from a TOML file when a TOML loader is available so that TOML-formatted configuration can be supported where available.",
    "As a store owner, I want class names (such as 'TestConfig') not to be injected into the app config so that only configuration key/value pairs are present and internal implementation details are not exposed.",
    "As a store owner, I want the app's secret_key to be set from configuration sources so that cryptographic operations (sessions, cookies) use the configured secret.",
    "As a store owner, I want environment variables with a specific prefix (default 'FLASK') to be imported into configuration so that I can configure the app via environment variables without importing unrelated variables.",
    "As a store owner, I want prefixed environment variable values to be parsed into appropriate Python types (booleans, ints, floats, lists, dicts) so that configuration values are usable without manual parsing.",
    "As a store owner, I want environment variables that do not match the configured prefix to be ignored when importing prefixed environment variables so that only intended settings are loaded.",
    "As a store owner, I want to be able to specify a custom prefix when importing environment variables so that I can use a different namespace for configuration in different deployments.",
    "As a store owner, I want keys in prefixed environment variables using a double underscore separator to be interpreted as nested configuration structures so that hierarchical configuration can be represented via environment variables.",
    "As a store owner, I want file-based configuration loading to accept a loader and a text/bytes mode option so that different file formats and encodings can be correctly handled during import."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_converters.py": [
    "As a developer, I want to register custom URL converters on the application's url_map under a name so that I can use <name:var> syntax in routes to parse custom parameter formats.",
    "As a developer, I want to implement to_python on a custom converter to transform a URL string into a Python object (e.g., splitting a comma-separated list into a Python list) so that view functions receive typed, ready-to-use values.",
    "As a developer, I want to implement to_url on a custom converter to convert Python objects into properly formatted URL segments so that url_for generates correct routes for those objects.",
    "As a developer, I want to call the base converter's to_url for each element when composing complex URL segments so that each element is escaped and encoded consistently.",
    "As a developer, I want url_for to use the converter's to_url logic when building URLs so that generated links match the same format the converter expects on incoming requests.",
    "As a developer, I want request and session to be available inside a converter's to_python method so that converters can perform context-aware parsing or validation based on the current request/session.",
    "As a developer, I want routes using custom converters to receive the converted values and return expected responses so that application behavior is consistent between direct requests and url_for-generated URLs."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_helpers.py": [
    "As a developer, I want flask.send_file to enable direct_passthrough by default so that large files can be streamed efficiently without loading them entirely into memory.",
    "As a developer, I want flask.send_file to set the correct MIME type based on the file extension so that clients receive the appropriate Content-Type and can handle the file correctly.",
    "As a developer, I want to be able to set the response's direct_passthrough to False so that the application can read the file data into memory when needed (for inspection or testing).",
    "As a developer, I want send_static_file and flask.send_file to have no cache max-age by default (cache_control.max_age is None) so that files are not cached unless I explicitly configure caching.",
    "As a developer, I want the SEND_FILE_MAX_AGE_DEFAULT configuration to control the default cache-control max_age for send_static_file and flask.send_file so that I can globally manage static file caching behavior.",
    "As a developer, I want send_static_file to accept Path-like objects that implement __fspath__ (such as pathlib.Path) so that filesystem path abstractions are supported.",
    "As a developer, I want to be able to override get_send_file_max_age(filename) in a Flask subclass so that I can return a custom max_age per file and customize caching behavior based on filename."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_instance_config.py": [
    "As a developer, I want the system to reject non-absolute instance_path values so that the application's instance location is unambiguous and valid.",
    "As a developer, I want the system to honor an explicitly provided absolute instance_path so that the application uses the specified instance directory.",
    "As a developer, I want an uninstalled (development) module's Flask app to default its instance_path to the project-level 'instance' folder so that configuration and instance files are stored alongside the project.",
    "As a developer, I want an uninstalled package's Flask app to default its instance_path to the project-level 'instance' folder so that package development uses a shared project instance directory.",
    "As a developer, I want a namespace package's Flask app to default its instance_path to the parent project directory's 'instance' folder so that namespace-contained packages share the project's instance location.",
    "As a developer, I want an installed module's Flask app to default its instance_path to a system 'var' directory named '<module>-instance' so that installed packages have a predictable, centralized instance location.",
    "As a developer, I want an installed package's Flask app to default its instance_path to a system 'var' directory named '<package>-instance' so that installed packages' instance data is placed in a consistent system-wide location."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_json_tag.py": [
    "As a developer, I want the TaggedJSONSerializer to round-trip data (dump and then load) unchanged so that serialized objects are faithfully restored to their original values.",
    "As a developer, I want the serializer to correctly handle common Python types (tuples, lists, bytes including non-ASCII, Markup, UUIDs, and timezone-aware datetimes) so that these objects can be serialized and deserialized reliably.",
    "As a developer, I want JSONTag subclasses to provide a unique key attribute so that the serializer can identify and dispatch tags unambiguously.",
    "As a developer, I want attempting to register a tag whose key already exists to raise a KeyError unless I explicitly force registration so that accidental key collisions are prevented.",
    "As a developer, I want to be able to force-register a tag and specify an index so that I can override existing tags and control tag matching precedence.",
    "As a developer, I want the TaggedJSONSerializer to maintain a mapping of tag keys to tag classes and an ordered list of tags so that tags can be looked up quickly and precedence is preserved.",
    "As a developer, I want the JSONTag base class methods (check, to_json, to_python) to raise NotImplementedError by default so that concrete tag implementations are required to implement them and the contract is explicit.",
    "As a developer, I want a custom JSONTag to be able to define a check(value) method to detect applicable values so that the serializer can select the correct tag for a given object.",
    "As a developer, I want a custom JSONTag to implement to_json(value) to produce a JSON-serializable representation and to_python(value) to reconstruct the Python object so that custom types can be serialized and deserialized.",
    "As a developer, I want tag implementations to be able to call the serializer's tagging helper when converting nested data (e.g. self.serializer.tag(inner)) so that nested values are serialized using the same tag rules.",
    "As a developer, I want invalid tag registration (such as registering a class that doesn't meet the JSONTag interface or lacks required attributes) to raise a TypeError so that only valid tags are accepted.",
    "As a developer, I want to be able to inspect the order of registered tags and insert tags at a specific position so that more specific tag checks can take precedence over more general ones."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_json.py": [
    "As a developer, I want the application to return a 400 Bad Request when it cannot decode JSON so that clients are informed the request body is malformed.",
    "As a developer, I want detailed JSON decode error messages to appear in 400 responses when DEBUG is True and TRAP_BAD_REQUEST_ERRORS is False so that I can diagnose malformed JSON during development.",
    "As a developer, I want detailed JSON decode error messages to be hidden in 400 responses when DEBUG is False so that error details are not exposed in production.",
    "As a client, I want the server to accept JSON requests with custom +json MIME types (for example application/x+json) so that non-standard but valid JSON content types are parsed correctly.",
    "As a developer, I want request.get_json() to parse the request body and return native Python types so that view functions can process JSON payloads directly.",
    "As a developer, I want flask.json.dump and flask.json.load to write to and read from file-like objects so that JSON data can be persisted and reloaded from streams.",
    "As a developer, I want the JSON provider's ensure_ascii setting to control whether non-ASCII characters are escaped or output as raw Unicode so that I can produce ASCII-only JSON when needed or readable Unicode when desired.",
    "As a client, I want flask.jsonify to serialize basic Python types (numbers, strings, booleans, None) and return responses with the application/json MIME type so that I receive correctly typed JSON responses I can parse back into equivalent values."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_logging.py": [
    "As a developer, I want the application's logger to be named \"flask_test\" so that the logger can be identified consistently in tests and runtime.",
    "As a developer, I want the application's logger default level to be NOTSET so that it inherits the root logger's behavior unless explicitly changed.",
    "As a developer, I want the application's logger to use the default_handler by default so that log messages have a consistent handling mechanism out of the box.",
    "As a developer, I want the application's logger level to become DEBUG when app.debug is True so that debug messages are captured during development.",
    "As a developer, I want the application to avoid attaching handlers to its logger when the root logger already has handlers so that duplicate message handling and handler proliferation are prevented.",
    "As a developer, I want error messages emitted during a request to be written to the WSGI errors_stream provided to the client so that request-specific error output can be captured or redirected.",
    "As a developer, I want the global WSGI errors stream to default to sys.stderr so that errors go to the standard error output when no custom stream is provided.",
    "As a developer, I want the WSGI errors stream to resolve to a request-specific errors_stream when set in the request context so that logging can be redirected per request.",
    "As a developer, I want the has_level_handler(logger) helper to report False when no applicable handlers exist and True when a handler on the root logger would handle the message so that I can determine whether log messages will be handled.",
    "As a developer, I want has_level_handler(logger) to account for logger.propagate being False and return False in that case so that the presence of handlers higher in the hierarchy is ignored when propagation is disabled.",
    "As a developer, I want the test environment to reset and restore logging root handlers and levels before and after each test so that tests run in isolation and do not leave global logging state modified."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_regression.py": [
    "As a developer, I want to register an error handler for a custom exception that returns the exception's attribute value so that error handlers can produce meaningful responses based on exception data.",
    "As a developer, I want raising abort(redirect(url_for('test'))) inside a view to produce an HTTP redirect response so that the client is redirected to the intended endpoint and normal request processing stops.",
    "As a developer, I want the redirect Location header to resolve to the 'test' endpoint (allowing either a relative path '/test' or an absolute 'http://localhost/test') so that redirects consistently point to the correct endpoint across different Werkzeug versions.",
    "As a developer, I want a request to the '/test' endpoint that raises the custom Foo exception to invoke the registered error handler and return the value '42' as the response body so that exception handling yields the expected output."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_reqctx.py": [
    "As a developer, I want teardown_request handlers to be executed when a request context is popped so that cleanup logic always runs after a request ends.",
    "As a developer, I want teardown_request handlers to receive None when no exception occurred during the request so that cleanup can assume a successful request state.",
    "As a developer, I want teardown_request handlers to receive the exception that occurred during the request so that cleanup logic can react to errors appropriately.",
    "As a developer, I want a previously raised and handled exception outside or inside a test request context not to be propagated to the teardown handlers so that teardown only reflects exceptions from the current request.",
    "As a developer, I want app.test_request_context(...) used as a context manager to automatically push and pop the request context so that tests do not need manual push/pop calls and teardown always runs.",
    "As a developer, I want manually calling ctx.push() and ctx.pop() to behave the same as using the context manager so that both usage patterns provide consistent request lifecycle behavior.",
    "As a developer, I want the test request context to use the application's SERVER_NAME (including port) when building external URLs so that url_for(..., _external=True) returns correct absolute URLs.",
    "As a developer, I want subdomain routes and the application's SERVER_NAME to be respected when generating external URLs so that urls for subdomain endpoints include the correct subdomain.",
    "As a developer, I want the request context to accept environment overrides for path, query string, method, data, headers, and remote address so that tests can accurately simulate different request scenarios.",
    "As a developer, I want the request's url_scheme to reflect the WSGI environment (e.g., 'http' or 'https') so that generated external URLs and request behavior match the intended scheme.",
    "As a developer, I want request-specific state (buffers, globals) to be isolated and cleared between request contexts so that tests and requests do not leak state into one another.",
    "As a developer, I want request context lifecycle and teardown behavior to be predictable regardless of whether exceptions are raised and handled within the context so that error handling and resource cleanup remain consistent."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_request.py": [
    "As an app administrator, I want the application to enforce MAX_CONTENT_LENGTH so that requests larger than the configured maximum are rejected and the 413 error handler can return a custom response.",
    "As an app administrator, I want request size and form limits to use the application's configuration when inside an app context so that the values of MAX_CONTENT_LENGTH, MAX_FORM_MEMORY_SIZE, and MAX_FORM_PARTS reflect site-wide settings.",
    "As an app administrator, I want sensible framework defaults applied when no app context or configuration is available (specifically MAX_CONTENT_LENGTH = None, MAX_FORM_MEMORY_SIZE = 500000, MAX_FORM_PARTS = 1000) so that the system has predictable limits even without app configuration.",
    "As a developer, I want to be able to override Request.max_content_length, Request.max_form_memory_size, and Request.max_form_parts on a Request instance so that individual requests can use custom limits regardless of the app context.",
    "As an app administrator, I want the application to accept requests only from hosts listed in TRUSTED_HOSTS (supporting exact names and leading-dot subdomain patterns) so that requests from untrusted hosts are rejected with a 400 response."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_session_interface.py": [
    "As a developer, I want open_session implementations to be able to call RequestContext.match_request() so that URL matching and request.endpoint are available while loading the session.",
    "As a developer, I want open_session to ensure request.endpoint is not None after matching so that session loading can safely rely on endpoint-specific logic.",
    "As a developer, I want to be able to assign a custom SessionInterface to app.session_interface so that the application can use custom session loading and saving behavior.",
    "As a developer, I want save_session to be implemented (even as a no-op) on a SessionInterface subclass so that the session lifecycle contract is satisfied and saving can be customized or intentionally skipped.",
    "As a developer, I want standard route handlers (e.g. GET \"/\") to continue returning a successful response when a custom SessionInterface is used so that normal request handling is not broken by custom session logic."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_signals.py": [
    "As an app developer, I want the template_rendered signal to be emitted when a template is rendered so that listeners can inspect which template was used and the rendering context.",
    "As an app developer, I want template listeners to receive a template object with a name property so that I can identify the exact template that was rendered.",
    "As an app developer, I want the before_render_template signal to be emitted before a template is rendered so that listeners can modify the template context prior to rendering.",
    "As an app developer, I want modifications made to the template context in a before_render_template listener to be reflected in the final rendered output so that listeners can change values seen by the user.",
    "As an app developer, I want request_started and request_finished signals to be emitted around each request so that I can instrument or observe the lifecycle of a request.",
    "As an app developer, I want the order of execution to be request_started signal, before_request handlers, the route handler, after_request handlers, then request_finished signal so that lifecycle hooks run in a predictable sequence.",
    "As an app developer, I want after_request handlers to receive the response and be able to modify its data before the response is finalized so that I can alter the output sent to the client.",
    "As an app developer, I want to be able to connect and disconnect signal handlers for a specific app so that tests and runtime code can attach listeners temporarily without leaking side effects.",
    "As an app developer, I want signal handlers to be invoked exactly once per relevant event (for example, one before_render_template per template render) so that event handling is deterministic and not duplicated."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_subclassing.py": [
    "As a developer, I want unhandled exceptions raised in route handlers to produce an HTTP 500 response so that clients receive a clear indication of an internal server error.",
    "As a developer, I want the default 500 response body to include the text \"Internal Server Error\" so that clients see a generic, non-sensitive error message.",
    "As a developer, I want to be able to subclass the Flask application and override log_exception to suppress logging so that I can control or prevent exception details from being recorded.",
    "As a developer, I want that when log_exception is overridden to do nothing, no exception output is written to a provided errors_stream so that sensitive exception details are not emitted to error output."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_templating.py": [
    "As a developer, I want context processors to inject values into the template context so that templates can access shared application data without having to pass it in every render call.",
    "As a developer, I want explicit variables passed to render_template or render_template_string to override same-named values from the application context (such as config) so that I can control and test template output precisely.",
    "As a developer, I want stream_template_string to render templates using provided variables and stream the result so that I can generate responses incrementally while using the same template context rules.",
    "As a developer, I want templates rendered inside an application context (but outside a request) to be able to access app.config and values provided by context processors so that I can render templates with application-level configuration and injected values even when no request is active.",
    "As a developer, I want templates rendered during an active request to have access to request, g, config, and session objects so that templates can display request-specific and session-specific information.",
    "As a developer, I want template variables to be auto-escaped by default but allow Markup (or explicitly marked safe content) to bypass escaping so that HTML is safe by default while still permitting intentional raw HTML output.",
    "As a developer, I want the template context to merge values from multiple sources (explicit render parameters, context processors, and app globals) with explicit render parameters taking precedence so that predictable shadowing/resolution of variables occurs."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_testing.py": [
    "As a developer, I want request URL generation to use SERVER_NAME and APPLICATION_ROOT from app.config so that generated URLs include the correct host, port, and application root.",
    "As a developer, I want the system to default request URLs to http://localhost/ when no SERVER_NAME or APPLICATION_ROOT is configured so that tests have a predictable base URL.",
    "As a tester, I want the test client to set flask.g.remote_addr to 127.0.0.1 by default so that request remote address is consistent across test runs.",
    "As a tester, I want the test client to set the default User-Agent header to Werkzeug/<version> so that user-agent-dependent behavior is predictable in tests.",
    "As a tester, I want to be able to modify client.environ_base to override REMOTE_ADDR and HTTP_USER_AGENT so that I can simulate requests from different IPs and user agents.",
    "As a tester, I want the client.open method to accept an EnvironBuilder and honor its constructed environment so that I can programmatically build custom request environments.",
    "As a tester, I want using the test client as a context manager (with client:) to preserve the application context across requests so that flask.g and other context globals are available between requests.",
    "As a developer, I want app.test_request_context() to create a request context whose request reflects the current app configuration so that code relying on request.url and other request properties behaves correctly during tests."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_user_error_handler.py": [
    "As a developer, I want the system to require an exception class (not an instance) when registering an error handler so that handlers are associated with exception types rather than specific exception instances.",
    "As a developer, I want the system to reject types that are not subclasses of Exception when registering error handlers so that only valid exception types can be handled.",
    "As a developer, I want registering an error handler by numeric status code to only accept codes that map to an HTTPException subclass so that handlers are bound to valid HTTP error types.",
    "As a developer, I want registering an invalid numeric status code (e.g., 999) to raise a clear ValueError instructing the use of an HTTPException subclass so that invalid handler registrations are prevented.",
    "As a developer, I want attempts to register a handler with an instance (e.g., CustomException()) to raise a TypeError explaining it is an instance and not a class so that incorrect API usage is caught early.",
    "As a developer, I want a handler registered for a parent exception class to handle subclasses for which no specific handler exists so that default behavior applies to related exceptions.",
    "As a developer, I want a handler registered for a specific child exception class to override the parent class handler so that specialized behavior is used for that subclass.",
    "As a developer, I want non-HTTP exceptions raised during request handling to be wrapped in an InternalServerError with the original exception preserved so that server errors are presented as HTTP 500 responses while retaining the original cause for debugging.",
    "As a developer, I want abort(500) (an explicit HTTP abort) to invoke the 500 error handler and be treated as a direct HTTP error rather than as a wrapped non-HTTP exception so that explicit aborts behave predictably.",
    "As a developer, I want error handler functions to receive the exception instance so that handlers can inspect properties (like original_exception) and return context-aware responses.",
    "As a developer, I want the application's testing mode to affect error handling behavior (e.g., enabling re-raising in tests vs. returning handler responses in non-testing mode) so that tests and production can differ appropriately."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/test_views.py": [
    "As a store owner, I want a view's declared HTTP methods (e.g. methods = ['GET','POST']) to determine which methods are accepted so that only intended actions are allowed on that endpoint.",
    "As a store owner, I want the system to automatically include HEAD and OPTIONS in the set of allowed methods when applicable so that clients can discover and use those protocol-level methods without explicit declaration.",
    "As a store owner, I want the application to return a 405 Method Not Allowed for requests using unsupported HTTP methods (e.g. PUT when only GET/POST are allowed) so that invalid requests are rejected.",
    "As a store owner, I want the OPTIONS response to include an Allow header listing all supported methods (including dynamically added ones like DELETE and implicit ones like HEAD and OPTIONS) so that clients can programmatically discover which methods are permitted.",
    "As a store owner, I want MethodView handlers (e.g. get(), post()) to be invoked for matching HTTP methods and their return values to be sent as the response body so that request handling is predictable and returns the intended content.",
    "As a store owner, I want the view instance to use its current view_class when dispatching requests (i.e. assigning view.view_class = Other should change behavior) so that implementations can be patched or swapped without re-registering routes.",
    "As a store owner, I want subclassing a MethodView to add additional HTTP methods (e.g. adding delete() in a subclass) so that inherited views can extend supported operations and those become discoverable to clients.",
    "As a store owner, I want HEAD requests to be supported automatically when a GET handler exists so that clients can request headers without needing a separate HEAD implementation."
  ],
  "/Users/richardlui/School/groundwork/flask/tests/type_check/typing_app_decorators.py": [
    "As a developer, I want to register after_request handlers (synchronous or asynchronous) that accept a Response and return a Response so that response post-processing always yields a valid Response object.",
    "As a developer, I want to register before_request handlers (synchronous or asynchronous) that take no arguments and return nothing so that I can run pre-request setup logic consistently.",
    "As a developer, I want to register teardown_appcontext handlers (synchronous or asynchronous) that accept an optional BaseException and return nothing so that I can clean up resources and inspect any exception that occurred during the application context.",
    "As a developer, I want the framework to accept both synchronous and asynchronous functions for request lifecycle decorators (@after_request, @before_request, @teardown_appcontext) so that I can implement handlers using either programming model as needed.",
    "As a developer, I want after_request handlers to be invoked after each request so that I can perform consistent post-request operations such as modifying or replacing the response."
  ]
}